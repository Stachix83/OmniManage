# Backend-Module (FastAPI, Datenbank, Authentifizierung)
fastapi[all]  # FastAPI für das Backend
uvicorn[standard]  # Uvicorn als ASGI-Server
sqlalchemy  # SQLAlchemy für die Datenbankverwaltung
pydantic[email]  # Pydantic für Datenvalidierung und -serialisierung
alembic  # Alembic für Datenbankmigrationen (optional, falls benötigt)
passlib[bcrypt]  # Passlib für sichere Passwort-Hashes
pyjwt  # Python-JWT für Token-basierte Authentifizierung
python-jose[cryptography]  # JWT-Token Verarbeitung
httpx  # HTTPX für asynchrone HTTP-Requests (z. B. API-Calls an externe Dienste)
python-dotenv  # dotenv für Umgebungsvariablen (falls du eine .env Datei nutzt)

# Frontend-Module (Flask, Formulare, Authentifizierung)
flask  # Flask für das Frontend
flask-cors  # CORS-Unterstützung für Flask
flask-login  # Flask-Login für Benutzerauthentifizierung in der WebUI
flask-wtf  # Flask-WTF für sichere Formulareingaben
requests  # Requests für einfache HTTP-Requests
werkzeug  # Optional: Werkzeug für erweiterte WSGI-Funktionalitäten

# Datenbank-Treiber (Optional: PostgreSQL oder MySQL)
psycopg2-binary  # Falls du PostgreSQL nutzt
# mysqlclient  # Falls du MySQL/MariaDB nutzt

# Optionale Module (Caching, Task-Management, Logging)
redis  # Optional: Redis für Caching oder WebSockets
celery[redis]  # Optional: Celery für Hintergrundaufgaben
loguru  # Optional: Loguru für bessere Logs

# Optional: Gunicorn für den produktiven Betrieb mit FastAPI
# gunicorn
